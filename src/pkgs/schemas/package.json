{
  "name": "@meu-caderno/caderno.schemas",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "dependencies": {
    "@typespec/compiler": "^1.13.0",
    "@typespec/json-schema": "^1.13.0"
  },
  "scripts": {
    "build": "nx build"
  },
  "nx": {
    "includedScripts": [],
    "namedInputs": {
      "default": [
        "{projectRoot}/src/**/*",
        "{projectRoot}/tspconfig.yaml"
      ]
    },
    "targets": {
      "build": {
        "inputs": [
          "default"
        ],
        "outputs": [
          "{projectRoot}/dist"
        ],
        "executor": "nx:run-commands",
        "options": {
          "cwd": "{projectRoot}",
          "commands": [
            "rm -rf ./dist",
            "mkdir -p ./dist/schemas",
            "tsp compile src/main.tsp --output-dir ./dist",
            "cp ./dist/schemas/schemas.json ./lib/schemas.json",
            "bunx --package quicktype@23.2.6 quicktype --src-lang schema './dist/schemas/schemas.json#/$defs/' --lang typescript-zod -o \"./lib/schemas.ts\""
          ],
          "parallel": false
        },
        "cache": true
      }
    }
  }
}